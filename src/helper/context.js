import { createContext } from "react";

export const NextDisabledContext = createContext({});
export const nameErrContext = createContext({});
export const lastNameErr = createContext({});
export const emailErr = createContext({});
export const phoneErr = createContext({});
